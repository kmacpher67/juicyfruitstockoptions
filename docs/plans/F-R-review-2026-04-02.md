# F-R Review — 2026-04-02

> **Purpose**: Snapshot analysis of `docs/features-requirements.md`. Summarizes item statuses, missing test coverage, effort estimates, and merge-ready prioritization guidance. Intended to be merged back into the master F-R (or referenced alongside it) after review.

> **Follow-up (2026-04-19):** F-R queue workflow planning for Juicys reviewed trades now has a dedicated contract and plan:
> [Juicy Follow-up/Review (F-R) Workflow](../features/juicy_fr_followup_review.md) and [Implementation Plan](implementation_plan-20260419-juicy-fr-followup-review.md).

---

## 1. Status Snapshot

### Counts by section

| Section | `[x]` Done | `[/]` In Progress | `[ ]` Open | `[!]` Blocked |
|---|---|---|---|---|
| 2. Infrastructure & Modernization | ~45 | 3 | ~30 | 0 |
| 3. Algorithmic Trading Engines | ~38 | ~18 | ~42 | 0 |
| 4. Dashboard & UX Features | 0 | 0 | ~18 | 0 |
| 5. Agentic AI & Intelligence | 1 | 0 | ~10 | 0 |
| 6. Risk Management | 1 | 0 | ~7 | 0 |
| **Total (est.)** | **~85** | **~21** | **~107** | **0** |

---

## 2. In-Progress Items Analysis (`[/]`)

All `[/]` items are partially done with known blockers. Listed with gap summary:

| ID / Name | What's Done | What's Missing |
|---|---|---|
| `ibkr-orders-009` | Orders Query ID field stub in settings | Flex order-history parser mapping (needs real Flex Orders report) |
| `ibkr-tws-ui-rt-004` (IBKR Pending Orders) | TWS open order capture, Mongo persist, API, view | `ibkr-orders-003` Flex backfill, `ibkr-orders-012` BAG leg UI, `ibkr-orders-013` visibility controls |
| `Portfolio Analytics` (NAVStats) | Nav KPI widgets, 1D/7D/30D cards | Date semantics labels, account-scoped filter wiring, compact footprint |
| `Portfolio View` filters | Expiring, Near Money, Account, STK toggle, Covered/Uncovered/Naked, Export | Row counter finalization, coverage regression test suite, live-grid undefined/NaN fixes (001-006) |
| `Portfolio Coverage Status Regression` | `portfolio-coverage-005` (flat rows) done | `portfolio-coverage-001` through `004` (calculation + regression tests) open |
| `Pending Order Aware Coverage` | 008 sub-items, pending_effect fields, badges | Detail-drawer UI follow-up |
| `Export` (Portfolio CSV) | 001-003 row alignment done | `portfolio-export-004` regression test missing |
| `Stock Analysis Ticker Click` | 6-tab modal, all APIs, StockGrid/PortfolioGrid wiring | All 40 analytics columns tab, header with full name/description links |
| `Stock Analysis Header` | Plan exists | Implementation not started (see `implementation_plan-20260328-stock_analysis_header.md`) |
| `Trade history / dividends` | Flex dividend query configured, DB query ID documented | `ibkr_service.py` parser, dividend collection, Trade History view integration, Portfolio view integration |
| `Recommendation Database` | Plan exists | Not persisting all roll/xdiv/top-10 signals to Mongo for outcome tracking |
| `Smart Roll Analysis UP value` | Plan exists (`implementation_plan-20260203-smart_roll_markov.md`) | UP return/yield in roll, Markov in scoring algo, Signals tab in frontend |
| `Markov Chains` | Backend service + API done | Frontend Ticker Modal integration, Smart Roll integration, Dashboard signals section |
| `PRICE ACTION` | Backend service, ZigZag/BOS/FVG done | Interactive chart overlay in Price Action tab of TickerModal |
| `X-DIV Strategy / events` | Scanner, ICS export, download endpoint | `ical` subscribe URL endpoint, only-STK filter bug, DB historical record |
| `News headlines and Sentiments UI` | Backend sentiment + LLM reasoning done | Frontend display of stored sentiment/impact in UI |
| `Logging` (backend) | Most services done | Frontend logging / React Error Boundaries |
| `Dividend Feed` | Sortable list API done | Yahoo link in ticker, empty Holdings/Predicted/Target fields bug, per-account call/put display |
| `Trading Agent` | Portfolio type-column links to Gemini tab | Full analysis context passing, agent chat in ticker modal |

---

## 3. Missing Test Coverage

### 3.1 Backend — Explicitly Required by F-R (not yet written)

| F-R ID | Description | Priority |
|---|---|---|
| `ibkr-tws-tests-nav-account-001` | Account-scoped NAV stats + timeframe source/date labeling (TWS vs Flex branches) | High |
| `ibkr-orders-005` | Order normalization regression: stock, single-leg option, roll-like paired orders | High |
| `portfolio-coverage-001..004` | Coverage calculation at `(account, underlying)` granularity; AMD 200 shares / -2 calls = Covered | High |
| `portfolio-export-004` | Portfolio CSV export stays aligned with on-screen filtered rows across all filter combos | Medium |
| `portfolio-live-grid-006` | Merged-source portfolio rows don't regress Type/Price/NAV% display when realtime data is present | Medium |
| Dividend history parser | Test `ibkr_service.py` parsing of new Dividend_Report Flex query and DB upsert | Medium |
| BAG order normalization | Validate `ibkr_orders` BAG parent + legs are stored correctly from `openOrder` callbacks | Medium |

### 3.2 Frontend — Large Gaps

Only **2 frontend test files** exist (`navStatsUtils.test.js`, `portfolioFilters.test.js`). Missing:

| Component / Util | What Should Be Tested |
|---|---|
| `portfolioFilters.js` (expand) | Pending-coverage badge rendering, `portfolio-export-004` row alignment |
| `OrdersView` / `?view=ORDERS` | Table renders, staleness indicator, refresh action |
| `TickerModal.jsx` | Tab switching, loading state, error fallback, header link rendering |
| `TradeHistory.jsx` | Time-window switching, per-account metric breakdown, RT freshness indicator |
| `NAVStats.jsx` | Account scope switching, date subtitle rendering per timeframe/source |
| `DividendScanner.jsx` | List renders, click-through to dividend analysis modal |
| `Dashboard.jsx` routing | `?view=` param switches the correct component |

### 3.3 Integration Test Gaps (no coverage at all)

- Portfolio TWS live-grid merged row rendering (`portfolio-live-grid-001..006` regressions)
- Dividend ingest pipeline (Flex parse → MongoDB → `/api/trades` with dividends)
- Smart Roll outcome tracking (roll chosen → persisted to Recommendation DB)
- NAV card account-scoped filtering end-to-end

---

## 4. Open Item Effort Estimation & Prioritization

### 4.1 Quick Wins — Complete First (< 1 day each)

These are tightly scoped, well-defined, and unblocked:

| Item | Effort | Notes |
|---|---|---|
| **`portfolio-live-grid-001..006`**: Fix `undefined`/`NaN%` display bugs in portfolio grid | XS (2–4h) | Pure frontend guard + null-coalescing; no new API needed |
| **`ibkr-tws-ui-nav-compact-001`**: Reduce NAV card vertical footprint | XS (1–2h) | CSS/padding only in `NAVStats.jsx` |
| **`portfolio-coverage-001..004`**: Add regression tests for coverage calculation | S (3–4h) | Tests only; logic is fully spec'd in F-R |
| **`portfolio-export-004`**: Add regression test for CSV export filter alignment | XS (2h) | One test in `test_portfolio_features.py` |
| **Stock Analysis bug fix**: Stock Analysis UI broke after 20260327 changes | S (3–5h) | High urgency; user reported as broken |
| **Vendor/ibapi commit + onboarding notes** (`vendor-ibapi` stub items) | XS (30m) | Git commit + 3-line README note |
| **`ibkr-orders-012`**: BAG combo leg row display (backend data exists) | S (4–6h) | Frontend-only: grouped roll view with leg rows |
| **`Stock Analysis Header`**: Add company name + link to modal header | S (4h) | Plan exists at `implementation_plan-20260328-stock_analysis_header.md` |
| **`events` ical subscribe URL endpoint** | S (3h) | One new FastAPI route returning `text/calendar`; ICS builder already exists |
| **`events` STK-only filter bug** | XS (1–2h) | Filter OPT symbols before yfinance fetch in corporate events/xdiv scanner |

### 4.2 Near-Term Completions — Short Sprint (1–3 days each)

Items where foundation is done and the remaining scope is well-bounded:

| Item | Effort | Notes |
|---|---|---|
| **NAV date semantics `nav-date-001/002/003`** + API metadata field | S–M (1–2d) | API change to `/portfolio/stats` + `NAVStats.jsx` subtitle rendering |
| **Account-scoped NAV `nav-account-001` (API + UI wiring)** | M (1–2d) | Add `account_id` query param to stats/nav/live; wire portfolio account dropdown |
| **`ibkr-tws-tests-nav-account-001`**: Write regression tests | S (3–5h) | After nav-account-001 is done |
| **Dividend Backend Parser**: Parse Dividend_Report Flex XML → `dividends` collection | M (1d) | Core data ingest; unblocks dividend Trade History + Portfolio view |
| **Dividend Trade History view**: Show dividend rows in `?view=TRADES` | S (4h) | Depends on dividend parser; add row type to existing trade table |
| **`Recommendation Database`**: Persist all roll/xdiv/top-10 signals | M (1d) | Mainly backend scheduler + Mongo upsert; plan exists |
| **`ibkr-tws-hardening-005`**: Auto-reconnect with bounded backoff | S (4h) | `IBKRTWSService` change only; well-defined behavior |
| **`Portfolio Row Counter`** finalization | XS (1–2h) | Toolbar count after filters applied; mostly working per F-R |
| **Frontend logging / React Error Boundaries** | S (3–4h) | Basic Error Boundary wrapping each major component |

### 4.3 Medium Effort — Require a Plan Sprint (3–7 days)

Items with meaningful scope that need an implementation plan or sub-decomposition before starting:

| Item | Effort | Notes |
|---|---|---|
| **Portfolio TWS Live Grid regression fixes** (`portfolio-live-grid-001..006`) holistic | M–L (3–5d) | Needs view-model normalization layer for Flex/TWS merged rows across all fields |
| **Smart Roll UP value + Markov in scoring** | M (3–5d) | `implementation_plan-20260203-smart_roll_markov.md` exists; needs execution |
| **Markov Frontend integration** (Signals tab in TickerModal) | M (2–3d) | Visualize Kalman/Markov state from `/api/analysis/signals/{ticker}` |
| **Stock Analysis — All 40 Analytics columns** in Analytics tab | M (2–3d) | Map existing API fields to tab; no new backend needed |
| **X-DTE XDTE options UI** (hold/wait/close actions, DTE bug fix) | M (3d) | `implementation_plan-20260203-xdte_autoroll.md` exists |
| **`ibkr-portal-003`**: Register portal into lifespan/scheduler vs on-demand | S–M (1–2d) | Architecture decision needed first |
| **`ibkr-tws-hardening-001..006`** (full hardening set) | M (3–4d) | Reconnect lifecycle, startup diagnostics, fallback routing |
| **Dividend Capture Analysis UI** (per-account, option positions, call/put) | M (2–3d) | Depends on dividend parser completion |
| **News/Sentiment UI** (surface stored sentiment + impact score in frontend) | M (2–3d) | Backend done; frontend display of sentiment cards + news tab |

### 4.4 Avoid / Needs Major Decomposition (Do Not Start Without Planning)

These items are too large, under-specified, or dependent on research/external systems. Each needs its own planning sprint with sub-item decomposition before any coding:

| Item | Why to Defer |
|---|---|
| **RAG System** | Requires vector DB selection, ingestion pipeline, chat UI — minimum 2-week project |
| **Backtesting Engine** | Separate system; requires OHLCV data, library selection, execution environment |
| **ML in the Loop** (8-step flow) | Research-heavy; no data pipeline foundation exists yet |
| **Stock Research via Gemini** | Open-ended scope; all sub-items are `[ ]` with no foundation |
| **Local Model Hosting** | Hardware/cost research prerequisite; no implementation path |
| **Personal Trading History** (journal, RAG, CSV import) | Large scope; overlaps backtesting |
| **Google Docs Migration** | Requires OAuth/Service Account setup; external dependency |
| **Backtesting Metric Stack** (Sharpe, Sortino, etc.) | Depends on backtesting engine |
| **IB Gateway Docker Container** (`ibkr-tws-gateway-001..004`) | Research shows TWS localhost already works; need to evaluate actual value-add first |
| **Agentic AI / LangChain orchestration** | Prerequisites (tools, data pipeline) not in place |
| **Interactive Graphs / Charting** | Library not chosen; requires Recharts/Chart.js selection + full chart component build |
| **Scheduler Control Panel UI** | Low value vs effort; websocket/SSE for log streaming is non-trivial |
| **Price Action interactive chart overlay** | Requires a charting library first; blocked by Interactive Graphs item |

---

## 5. Recommended Merge Updates to `features-requirements.md`

The following edits should be applied when merging this review back into the master F-R:

### 5.1 New items to ADD

```markdown
## 0. Bugs, Fixes, & Maintenance
- [ ] **stock-analysis-broken-202603**: Stock Analysis "Run Live Analysis" feature
  broken after 2026-03-27 changes. AI_Stock_Live_Comparison XLSX and onscreen grid
  no longer populate. Investigate `stock_live_comparison.py` and
  `app/services/stock_live_comparison.py` for root cause.

- [ ] **stock-analysis-report-churn-20260402**: Stock Analysis keeps generating
  multiple `AI_Stock_Live_Comparison_*.xlsx` files in the same day (many
  empty/partial). Enforce trigger-based report file policy and remove duplicate
  run enqueue.
  - [ ] `/run/stock-live-comparison` enqueues exactly one background job.
  - [ ] Manual **Run Live Comparison** creates a new timestamped XLSX.
  - [ ] Scheduled run creates at most one new XLSX per calendar day.
  - [ ] Background ticker sync/update paths refresh data without creating a new XLSX.

- [ ] **portfolio-live-grid-undefined-values**: Portfolio grid renders JavaScript
  literal `undefined` for Price, Value, Basis, and Unrealized PnL when merged
  live+Flex rows have missing fields. Guard all currency/number renders. Tracked
  under `portfolio-live-grid-001..006`.

- [ ] **events-stk-filter-bug**: Corporate events/xdiv scanner fetches yfinance
  for OPT contract symbols (e.g., `AMD 260220C00235000`), causing HTTP 404 errors.
  Filter to underlying STK symbols only before any yfinance fetch.
```

### 5.2 Items to mark `[/]` (promoted from `[ ]` — partial work visible)

- `ibkr-tws-ui-nav-date-001/002/003`: NAV date semantics — discussion and requirements fully spec'd; no code yet but API contract is clear.
- `portfolio-live-grid-001..006`: Regressions documented in `docs/features/portfolio_tws_live_grid_regressions_20260331.md`; no fix yet.

### 5.3 Items to mark `[x]` (confirmed done — may have been missed)

- `portfolio-show-stk-001..004`: All implemented per 2026-04-02 changelog.
- `portfolio-pending-coverage-001..008`: All backend fields implemented per 2026-04-02 changelog.
- `ibkr-tws-positions-001`: Contract-level position key fix confirmed 2026-04-02.
- `portfolio-coverage-005`: Flat-row coverage blank confirmed 2026-04-02.

### 5.4 Items to annotate as `[!] Needs Decomposition` before implementation

- `ibkr-tws-gateway-001..004`: Add note — "Evaluate whether standalone TWS on host already satisfies all use cases; decompose only if containerized gateway adds measurable reliability."
- `RAG System`: Add note — "Min viable scope: ChromaDB + docs/ ingestion only. Full chat interface is a separate item."

---

## 6. Sprint Recommendation

Given the current backlog, this order maximizes visible progress and clears the most debt:

**Sprint 1 (This Week — Quick Wins)**
1. Fix Stock Analysis bug (broken feature, urgent)
2. Fix `portfolio-live-grid-001..006` undefined/NaN display
3. Add `portfolio-coverage-001..004` regression tests
4. Fix `events` STK-only filter bug in xdiv/corporate events scanner
5. `ibkr-tws-ui-nav-compact-001` NAV card vertical footprint
6. `portfolio-export-004` export regression test

**Sprint 2 (Next Week — Foundation Completions)**
1. NAV date semantics + API metadata fields (`nav-date-001/002/003`)
2. Account-scoped NAV API + UI wiring (`nav-account-001`)
3. Dividend backend parser (`ibkr_service.py` → `dividends` collection)
4. `ibkr-tws-hardening-005` auto-reconnect backoff
5. `Stock Analysis Header` company name/links (plan exists)

**Sprint 3 (Week 3 — Analysis & Signals)**
1. Dividend Trade History view + Portfolio view integration
2. Smart Roll UP value + Markov scoring (`implementation_plan-20260203-smart_roll_markov.md`)
3. All 40 Analytics columns in TickerModal Analytics tab
4. Recommendation Database persistence for roll/xdiv signals
5. Frontend logging / React Error Boundaries

**Defer to Backlog**
- Backtesting Engine, RAG System, ML pipeline, Local LLM, Google Docs, IB Gateway container, Charting/Interactive Graphs, Scheduler Control Panel, Agentic AI orchestration

---

*Generated: 2026-04-02 | Source: `docs/features-requirements.md` + `tests/` + `frontend/src/components/*.test.*`*
