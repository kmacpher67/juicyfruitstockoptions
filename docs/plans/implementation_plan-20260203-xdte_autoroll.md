# Implementation Plan - X-DTE Options UI & Auto-Roll Evaluation

## Goal Description
Implement the User Interface for the "Options Due in X Days" signal and the "Auto-Roll Evaluation" feature. 
The backend signals (expiring options) are already generated by `ExpirationScanner`. This plan focuses on surfacing these signals in the Dashboard and providing an interactive "Smart Roll" analysis tool to help the user decide whether to Roll, Hold, or Close.

## User Review Required
> [!NOTE]
> This feature relies on the existing `JuicyOpportunity` persistence. We will be filtering for `trigger_source="ExpirationScanner"`.

## Proposed Changes

### Frontend Components
### Frontend Components
#### [MODIFY] [frontend/src/components/AlertsDashboard.jsx](file:///home/kenmac/personal/juicyfruitstockoptions/frontend/src/components/AlertsDashboard.jsx)
- Update `fetchAlerts` (or add parallel fetch) to get `JuicyOpportunity` items where `source="ExpirationScanner"`.
- Display a new section (or distinct alert cards) for "Expiring Options".
- **[UPDATE]** Ensure "Dividend Capture Opportunities" section is hidden or shows a "No Opportunities" message if empty, rather than being a large empty box.
- Card details: Symbol, DTE, Strike, Current Price.
- Action: "Analyze Roll" button.

#### [MODIFY] [frontend/src/components/RollAnalysisModal.jsx](file:///home/kenmac/personal/juicyfruitstockoptions/frontend/src/components/RollAnalysisModal.jsx)
- **Modal Component**: Opens when "Analyze Roll" is clicked.
- **Inputs**: (Pre-filled) Symbol, Strike, Expiration.
- **API Call**: POST `/analysis/roll` (endpoint to be enhanced to return more context).
- **[NEW] Persistence**: Should save the selected roll or the analysis session?
    - *Decision*: For now, "Select" will log the decision to console/toast. Future: Save to `TradePlan` collection. 
    - The "Spinner" indicates the backend is calculating fresh real-time data. This is correct behavior.
- **[NEW] Display Enhancements**:
    - **Original Position Breakdown**: 
        - Show Type (Call/Put), Side (Short), Strike, Expiry.
        - Show Cost Basis (if available in DB) and Unrealized P/L context.
    - **Roll Candidates Table**:
        - **Yield/Return**: Calculate and show "Annualized Return if Called" and "Downside Protection" (Net Credit / Strike).
        - **Sequence Explained**: Add tooltip or text explaining "Buy to Close X + Sell to Open Y = Net Credit Z".
        - **Scenarios**: Show "Breakeven" price. 
    - **No Suitable Rolls**:
        - If no rolls found (or all negative scores), display generic "Action Card":
            - **WAIT**: "Premium might improve closer to expiry."
            - **HOLD**: "Keep position if OTM."
            - **CLOSE**: "Buy to Close now at $X market price."
- **Action**: "Select" button.
    - *Update*: Clicking "Select" should highlight the row and maybe enable an "Execute" or "Save Plan" button. For V1, it will show a Toast confirmation: "Roll Selected: [Details]".

### Backend Services (Minor Adjustments)
#### [CHECK] [app/services/roll_service.py](file:///home/kenmac/personal/juicyfruitstockoptions/app/services/roll_service.py)
- Ensure `find_rolls` handles edge cases (zero volume, wide spreads) gracefully. (Existing code looks robust enough for V1).

#### [MODIFY] [app/services/expiration_scanner.py](file:///home/kenmac/personal/juicyfruitstockoptions/app/services/expiration_scanner.py)
- **Fix DTE Calculation**:
    - Change from `datetime.utcnow()` to `datetime.now()` (to match user perception).
    - Use `.date()` difference to avoid timestamp truncation (ensure partial days count as full calendar days).
    - Formula: `(exp_dt.date() - now.date()).days`.

### Frontend Layout Refinements
#### [MODIFY] [frontend/src/components/AlertsDashboard.jsx](file:///home/kenmac/personal/juicyfruitstockoptions/frontend/src/components/AlertsDashboard.jsx)
- **Compact Layout**:
    - Reduce card padding (padding-y).
    - Optimize vertical spacing between elements inside the card.
    - Ensure grid density is appropriate.
    - Verify "Clock" icon alignment to save space.

## Verification Plan

### Automated Tests
- **Frontend**: N/A (We rely on manual verification for UI in this project usually, unless Jest is set up).
- **Backend**:
    - Create unit test for `RollService.find_rolls` with mocked market data to ensure it returns expected structure for the UI.
    - Command: `pytest tests/test_roll_service.py` (Need to create if missing).

### Manual Verification
1.  **Trigger Scan**: Run `scan_portfolio_expirations` via script or wait for scheduler (can force via temporary route or python shell).
2.  **Verify Alert**: Check "AlertsDashboard" for "Expiring Option" cards.
3.  **Test Modal**: Click "Analyze Roll".
4.  **Verify Suggestions**: Ensure roll candidates appear with scores and valid net credits.
5.  **Dividend Check**: Test on a ticker with upcoming dividend (mock data if needed) to verify "Dividend Risk" flag.
